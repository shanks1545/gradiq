services:
  - type: web
    name: gradiq
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: GRADIQ_SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.12"
